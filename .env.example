# EC2 Dev Rig Configuration
# Copy this file to .env and customize as needed

# Instance settings
INSTANCE_NAME=dev-rig
INSTANCE_TYPE=t3.medium    # t2.micro for free tier
VOLUME_SIZE=20             # GB - minimum 20GB recommended
OS=al2023                  # al2023 or ubuntu
AWS_REGION=us-east-1

# Tailscale (optional but recommended)
# Create a reusable + ephemeral key at: https://login.tailscale.com/admin/settings/keys
# TAILSCALE_AUTHKEY=tskey-auth-xxxxx

# Private mode (requires Tailscale)
# auto = private when TAILSCALE_AUTHKEY is set (default)
# true = always private (no port 22, Tailscale required)
# false = always open port 22
PRIVATE_MODE=auto

# Bedrock access (enabled by default)
ENABLE_BEDROCK=true
IAM_ROLE_NAME=dev-rig-bedrock-role

# Security group and key names (usually don't need to change)
SECURITY_GROUP_NAME=dev-rig-sg
KEY_NAME=dev-rig-key
